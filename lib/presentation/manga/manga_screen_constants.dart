enum DownloadAction {
  next1Chapter,
  next5Chapters,
  next10Chapters,
  next25Chapters,
  unreadChapters,
}

enum EditCoverAction {
  edit,
  delete,
}

enum MangaScreenItem {
  infoBox,
  actionRow,
  descriptionWithTag,
  chapterHeader,
  chapter,
}
